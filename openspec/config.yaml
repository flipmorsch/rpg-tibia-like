schema: spec-driven

# Project context (optional)
# This is shown to AI when creating artifacts.
# Add your tech stack, conventions, style guides, domain knowledge, etc.
# Example:
#   context: |
#     Tech stack: TypeScript, React, Node.js
#     We use conventional commits
#     Domain: e-commerce platform

rules:
  proposal:
    - "Must include a 'Testing Strategy' section explaining how the new feature will be verified."
  tasks:
    - "Must include at least one task for automated unit or integration tests (e.g., '- [ ] Add tests for...')."
