{
  "name": "client",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "ui-dist/index.cjs.js",
  "collection": "ui-dist/collection/collection-manifest.json",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "dev:ui": "stencil build --dev --watch",
    "build:ui": "stencil build",
    "build": "npm run build:ui && tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "shared": "^1.0.0"
  },
  "devDependencies": {
    "@stencil/core": "^4.18.2",
    "jsdom": "^29.1.1",
    "typescript": "^5.3.3",
    "vite": "^5.0.12",
    "vitest": "^4.1.7"
  }
}
